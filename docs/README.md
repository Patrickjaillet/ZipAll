# docs

Holds documentation assets referenced from the root [`README.md`](../README.md).

- **`screenshot.png`** — a screenshot of the main window's Compress tab,
  captured from a real Windows desktop session running a built copy of
  ZipAll. Re-capture and overwrite this file whenever the main window's
  layout changes meaningfully.
